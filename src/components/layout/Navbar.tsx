import { Download, Menu, X } from 'lucide-react'
import { useEffect, useState } from 'react'
import { siteConfig } from '../../config/site'
const links=[['About','about'],['Projects','projects'],['Experience','experience'],['Skills','skills'],['Contact','contact']]
export function Navbar(){const [open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(scrollY>24);onScroll();addEventListener('scroll',onScroll,{passive:true});return()=>removeEventListener('scroll',onScroll)},[]);return <header className={`navbar ${scrolled?'is-scrolled':''}`}><nav className="nav-inner" aria-label="Primary navigation"><a href="#top" className="wordmark" onClick={()=>setOpen(false)}><span>AD</span>{siteConfig.name}</a><button className="menu-button" onClick={()=>setOpen(v=>!v)} aria-expanded={open} aria-controls="nav-menu" aria-label={open?'Close menu':'Open menu'}>{open?<X/>:<Menu/>}</button><div id="nav-menu" className={`nav-links ${open?'is-open':''}`}>{links.map(([label,id])=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}>{label}</a>)}<a className="nav-resume" href={siteConfig.resumePath} target="_blank" rel="noreferrer"><Download size={15}/> Resume</a></div></nav></header>}
