import { experience } from '../../data/experience'
import { SectionHeading } from '../ui/SectionHeading'
import { TechBadge } from '../ui/TechBadge'
export function Experience(){return <section id="experience" className="section"><div className="container"><SectionHeading eyebrow="03 / Experience" title="Building software in real operational environments."/><div className="timeline">{experience.map((item,i)=><article className="timeline-item reveal" key={item.role}><div className="timeline-marker"><span>{String(i+1).padStart(2,'0')}</span></div><div className="timeline-meta">{item.dates&&<time>{item.dates}</time>}<h3>{item.role}</h3>{item.company&&<p>{item.company}</p>}</div><div className="timeline-content"><p className="experience-description">{item.description}</p><ul>{item.responsibilities.map(r=><li key={r}>{r}</li>)}</ul>{item.technologies&&<div className="badge-row">{item.technologies.map(t=><TechBadge key={t}>{t}</TechBadge>)}</div>}</div></article>)}</div></div></section>}
